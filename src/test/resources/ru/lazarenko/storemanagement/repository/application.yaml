spring:
  profiles:
    active: ${PROFILE:local}
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        id:
          new_generator_mappings: true

  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/changelog.xml

  mail:
    host: smtp.yandex.ru
    username: bot.api.afalin@yandex.ru
    password: xiwrcjfakmbrprju
    port: 465
    protocol: smtps

upload:
  path: /C:/Documents/upload

mail:
  debug: true

app:
  initial:
    setup: false

logging:
  level:
    org:
      thymeleaf: DEBUG

